<div class="tweet-content">
  <header class="tweet-header">
    <a href="http://twitter.com/{{user.screenName}}">
      <img class="avatar" src="{{user.profileImageUrl}}" alt="Paul Miller" />
      <strong class="tweet-author-full-name">
        {{user.name}}
      </strong>
      <span class="username tweet-author-username">
        @{{user.screenName}}
      </span>
    </a>
  </header>
  <p class="tweet-text">{{text}}</p>
  <footer class="tweet-footer"></footer>
</div>
